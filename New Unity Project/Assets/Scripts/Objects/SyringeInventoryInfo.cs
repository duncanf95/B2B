using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SyringeInventoryInfo : MonoBehaviour
{
    // Start is called before the first frame update
    public bool selected;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
